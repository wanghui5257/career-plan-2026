server:
  port: 9997
  servlet:
    context-path: /back-server

spring:
  application:
    name: career-plan-2026-staging
  security:
    enabled: false  # Staging 环境禁用 Security，方便 API 测试
  datasource:
    url: jdbc:mysql://47.115.63.159:3306/career_plan_staging?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: career_plan_staging
    password: xASCetc8LSc24NjA
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  jackson:
    default-property-inclusion: non_null

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when_authorized
      probes:
        enabled: true

jwt:
  secret: v9Kx+L2RqP7mZ4wE1tY6uI8oA3sD5fG0hJ9kL1mN4pQ=
  expiration: 86400000

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.career.plan: DEBUG
    org.springframework.security: DEBUG
